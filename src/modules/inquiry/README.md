# Inquiry Module

Adapter over V1 `leads` collection with website scoping and contact linking.

## Public APIs

- `POST /api/v2/websites/:slug/inquiries` (public)
- `GET/PATCH /api/v2/websites/:slug/inquiries/:id` (admin)
